Como compilar

No terminal, dentro da pasta do projeto:

g++ main.cpp scanner.cpp -o compiler

O programa espera um arquivo como entrada:

./compiler nome_arquivo.cmm

Exemplo:

./compiler teste.cmm

Exemplo de entrada

Arquivo teste.cmm:

int a = 10;
a = a + 5;
